# Frontend API base URL
# Leave empty (recommended) so the browser calls same-origin /api/*
# and Vite proxies to the backend in development.
# VITE_API_BASE=
#
# Only set this if the API lives on a different host:
# VITE_API_BASE=http://localhost:5000
# VITE_API_BASE=https://api.yourdomain.com
